export interface ICharacters {
    id: number,
    name: string,
    characters: string,
    image: string,
    status: string,
    origin: {
        name: string
    }
    location: {
        name: string
    }
}

